export function run(client: Discord.Client, message: Discord.Message, args: string[]): Promise<Discord.Message>;
export namespace config {
    const name: string;
    const description: string;
}
import Discord = require("discord.js");
